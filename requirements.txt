# ============================================================
# PRODUCTION REQUIREMENTS — Smart Data Cleaner (Render Deploy)
# Python 3.11.6
# ============================================================

# --- Core Web Framework ---
Flask==3.0.0
Flask-CORS==4.0.0
Werkzeug==3.0.1
gunicorn==21.2.0

# --- Data Processing ---
pandas==2.1.4
numpy==1.26.4
openpyxl==3.1.5

# --- Configuration & Security ---
python-dotenv==1.0.0
PyJWT==2.8.0
cryptography==41.0.7

# --- Validation ---
marshmallow==3.20.1
email-validator==2.1.0

# --- Rate Limiting ---
Flask-Limiter==3.5.0

# --- Secure Headers ---
secure==0.3.0

# --- Structured Logging ---
python-json-logger==2.0.7

# --- Error Tracking ---
sentry-sdk==1.38.0

# --- Monitoring ---
prometheus-client==0.19.0

# --- Unstructured Data Processing (lightweight stack) ---
# PDF text extraction (fast, no ML models required)
PyMuPDF==1.25.1

# OCR Engine (optional — requires system tesseract binary)
pytesseract==0.3.13
Pillow==11.1.0

# Fuzzy String Matching (ICD-10 disease name matching)
thefuzz[speedup]==0.22.1

# Medical Spell Correction
symspellpy==6.9.0
