# ============================================================
# REQUIREMENTS — Medicl Smart Data Cleaner
# Python 3.10+  (tested on 3.11)
# ============================================================
# Install: pip install -r requirements.txt

# ── Core Web Framework ──────────────────────────────────────
Flask==3.0.0
Flask-CORS==4.0.0
Werkzeug==3.0.1

# ── Data Processing ─────────────────────────────────────────
pandas==2.1.4
numpy==1.26.4
openpyxl==3.1.5          # Excel read/write

# ── ML Imputation (optional — used by ml_impute_missing op) ─
scikit-learn==1.4.2

# ── PDF & Unstructured Text Extraction ──────────────────────
PyMuPDF==1.25.1           # Fast PDF text extraction

# ── OCR (optional — requires system Tesseract binary) ───────
# pytesseract==0.3.13
# Pillow==11.1.0

# ── Medical NLP ─────────────────────────────────────────────
thefuzz[speedup]==0.22.1  # Fuzzy ICD-10 matching
symspellpy==6.9.0         # Medical spell correction

# ── Configuration ───────────────────────────────────────────
python-dotenv==1.0.0

# ── Production Server ───────────────────────────────────────
gunicorn==21.2.0
